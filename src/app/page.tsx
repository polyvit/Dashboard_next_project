import Main from "@/components/Main/Main";
import styles from "./page.module.scss";

const Home: React.FC = () => {
  return (
    <main className={styles.main}>
      <Main />
    </main>
  );
};

export default Home;
